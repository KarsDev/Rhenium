package me.kuwg.re.token;

public enum TokenType {
    IDENTIFIER,
    INDENT,
    DEDENT,
    NEWLINE,
    NUMBER,
    STRING,
    CHARACTER,
    DIVIDER,
    KEYWORD,
    OPERATOR,
    BOOLEAN,
    EOF,
}
